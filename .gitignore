# OS / editor cruft
.DS_Store
Thumbs.db
*.swp

# Node (none committed; guard against accidental installs)
node_modules/

# Local working notes — personal memo, not published to the public plugin repo
CLAUDE.md
